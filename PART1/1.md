Part 1: Conceptual & Output Prediction

Don't run these in the interactive shell. Try to figure out the output in your head first, then verify.

Challenge 1: What is the exact output of this code, and what data type is the final result?
python
 
  
 
 
a = 10
b = 3
c = a / b
d = a // b
e = a % b
print(str(c) + " " + str(d) + " " + str(e))
 
 

Challenge 2: What is the output of this expression?
python
 
  
 
 
print(2 + 3 * 4 ** 2 - 15 // 4)
 
 

Challenge 3: Will this code run without an error? If yes, what is the output? If no, why not and how would you fix it using a function from Chapter 1?
python
 
  
 
 
score = 85
print("You scored " + score + " points!")
 
 

Challenge 4: Explain the difference between what happens here:
python
 
  
 
 
# Scenario A
spam = 10
spam + 1
print(spam)

# Scenario B
eggs = 10
eggs = eggs + 1
print(eggs)
 
 
Part 2: "Spot the Bug" & Code Repair

The following snippets contain errors based on Chapter 1 rules. Find the errors and fix them.

Challenge 5: The developer wants to calculate the area of a rectangle (width * height) and print a nice message.
python
 
  
 
 
width = input("Enter the width: ")
height = input("Enter the height: ")
area = width * height
print("The area is: " + area)
 
 

Challenge 6: The developer wants to create a variable for a user's account balance.
python
 
  
 
 
account balance = 500.50
2nd_deposit = 150.00
account balance = account balance + 2nd_deposit
print(account balance)
 
 

Challenge 7: The developer wants to print the word "Hello" 5 times on the same line.
python
 
  
 
 
greeting = 'Hello'
print(greeting + 5)
 
 
Part 3: Mini-Projects (Write from scratch)

Create a new Python file for each of these. Use comments to explain your logic.

Challenge 8: The Perfect Tip Calculator
Write a program that:

    Asks the user for the total cost of a meal (using input()).
    Asks the user for the percentage tip they want to leave (e.g., 15, 20).
    Calculates the exact tip amount (as a float).
    Calculates the total bill (meal + tip).
    Prints the tip amount and the total bill. 
    Constraint: Since input() returns a string, you must convert the inputs to numbers. Figure out whether to use int() or float(). When printing, format the numbers so they look like currency (e.g., concatenate a $ sign).

Challenge 9: The "Spam" Generator
Write a program that:

    Asks the user for their favorite word.
    Asks the user for a number.
    Prints their favorite word replicated by that number (all on one line).
    Prints the total number of characters in that final massive string.
    Hint: You will need string replication, len(), and type conversion.

Challenge 10: The Average Age Calculator
Write a program that:

    Asks the user for the ages of three different people (three separate input() calls).
    Calculates the sum of their ages.
    Calculates the average age (sum divided by 3).
    Prints the average age.
    Using round(), print the average age rounded to 1 decimal place.
    Using int(), print the average age rounded down to the nearest whole integer.


## Note: These challenges are generated by flagship ai models to test your understanding.